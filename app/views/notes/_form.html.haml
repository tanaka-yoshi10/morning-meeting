%h1= l @note.date, format: :long

= simple_form_for [@user, @note] do |f|
  = f.input :date, as: :hidden
  = f.input :content, as: :text, input_html: {rows: 10}
  = f.button :submit

= link_to 'Back', root_path(date: @note.date)
